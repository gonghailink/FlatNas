# v1.0.27-dev

## 变更
- Docker 组件外网地址打开不再强制追加容器端口
- 卡片右键菜单新增“外网访问”，支持直接打开 `item.url`

## 修复与优化
- 类型检查与代码规范通过
- 开发模式下热更新验证行为正确

## 升级指引
- 如需在 Docker 外网访问中使用端口，请在外网地址中自行包含端口（例如 `nas.example.com:8443`）
- 卡片右键菜单需登录才能显示；外网访问不依赖登录权限

